#include<stdio.h>
int main()
{
	int decimalnumber;
	printf("enter a decimalnumber");
	scanf("%d",& decimalnumber);
	printf("the decimal number in %d in octal format is:%o|n",decimalnumber,decimalnumber);
	
}